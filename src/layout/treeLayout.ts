import { DataType } from "../App";

export function layoutTree(data: DataType) {
  const { startNode, nodes, links } = data;
  // TODO: perform tree layout
  console.log("layoutTree() called");
  console.log(data);
}
